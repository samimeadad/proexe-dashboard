import React from 'react';
import Header from '../Header/Header';

const Dashboard = () => {
    return (
        <div>
            <Header></Header>
        </div>
    );
};

export default Dashboard;